get_consumed_energy_and_prices:
  fields:
    date:
      required: false
      example: "2023-01-01"
      selector:
        date:

force_check_latest_month:
force_update_current_year:
  fields:
    day_level:
      example: true
      default: false
      selector:
        boolean: