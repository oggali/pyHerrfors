get_consumed_energy_and_prices:
#  target:
#    entity:
##      integration: herrfors
#      domain: pyherrfors
    date:
      required: false
      example: "2023-01-01"

force_check_latest_month:

force_update_current_year:
  fields:
    day_level:
      example: True
      # The default field value
      default: False
      # Selector (https://www.home-assistant.io/docs/blueprint/selectors/) to control
      # the input UI for this field
      selector:
        select:
          options:
            - True
            - False